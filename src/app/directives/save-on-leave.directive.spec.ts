import { SaveOnLeaveDirective } from './save-on-leave.directive';

describe('SaveOnLeaveDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveOnLeaveDirective();
    expect(directive).toBeTruthy();
  });
});
